<html>
    <body>
        
        <h1>movies List</h1>
        <ul>
            <% movies.forEach(function(movie) { %>
                <li><a href="/movies/editMovie/<%= movie._id %>" ><%= movie.name %></a></li>
            <% }) %>
        </ul>
    </body>
</html>